<?php

namespace {{ namespace }};

use {{ namespace }}\Queries\{{ class }}Query;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Routing\Controller;

/**
 * @ai-agent For a list-view island, follow `base-datagrid-recipe.md` in the
 * islands-datagrid-development skill.
 */
class {{ class }}IslandController extends Controller
{
    public function __construct(private readonly {{ class }}Query $query)
    {
    }

    public function data(Request $request): JsonResponse
    {
        $this->authorizeAccess();

        return response()->json(['data' => $this->query->data($request)]);
    }

    /**
     * Guard every endpoint of this island. The middleware settles who is logged in, never
     * who may see this island — left empty, every signed-in user gets what it returns.
     */
    private function authorizeAccess(): void
    {
        //
    }
}
